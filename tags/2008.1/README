Installation is simple. To build, simply type:

$ qmake
$ make

On some distrobutions, the QT4 qmake is called "qmake-qt4," so on these you 
would type:

$ qmake-qt4
$ make

Finally, to install into "/usr/bin":

# make INSTALL_ROOT=/usr/ install


Enjoy!
